### **핵심 체크리스트** :white_check_mark:

#### :one: Infrastructure Layer (3개)
- [△] 기존 설계된 테이블 구조에 대한 개선점이 반영되었는가? (선택)
    - 판매율 높은 상품 집계시, 상품 정보를 꼭 가져오지 않더라도 집계가 가능하도록 반정규화를 다소 적용하였습니다.
- [O] Repository 및 데이터 접근 계층이 역할에 맞게 분리되어 있는가?
    - 멘토링 때, JPA를 적극 활용하라고 하셨지만,, 이미 인터페이스-구현체로 JPA 계층을 분리하려고 지난주에 작성해서,,, JPA의 강점을 최대한 살리진 못했지만,, 이 부분은 해결하였습니다.
- [O] MySQL 기반으로 연동되고 동작하는가?
    -  탬플릿에 존재하는 docker 와 properties 파일 기반의 DB를 연동하였습니다.

#### :two: 통합 테스트 (2개)
- [△] infrastructure 레이어를 포함하는 통합 테스트가 작성되었는가?
    - 각각 도메인의 Repository 테스트를 모두 작성하긴 하였으나,, sql 을 통한 더미데이터를 적극적으로 활용하진 못하였습니다.
- [O] 핵심 기능에 대한 흐름이 테스트에서 검증되었는가?
    - 지난 주 피드백을 기반으로,, 도메인->레포지토리->서비스->파사드-> 컨트롤러 계층의 테스트 코드를 모두 작성하려 노력하였습니다..
    - 부족한 점이 있었다면,, 아마 시간이 모자라서 커버를 못한 거로 간주해주시면 감사하겠습니다 :d

#### :three: DB 성능 최적화 (3개)
- [O] 조회 성능 저하 가능성이 있는 기능을 식별하였는가?
    - 판매율 높은 상품 조회 쿼리에 대한, 인덱스 설정 기준을 잡으려고 하였습니다.
    - 관련 설명 작성 : [[판매율 상위 상품 인덱스 설정해보기]](https://github.com/HangHae-Study/e-commerce-/blob/step08/docs/index/01_%EC%9D%B8%EB%8D%B1%EC%8A%A4_%EC%84%A4%EC%A0%95.md)
- [O] 쿼리 실행계획(Explain) 기반으로 문제를 분석하였는가?
- [△] 인덱스 설계 또는 쿼리 구조 개선 등 해결방안을 도출하였는가?
---

작업 내역 🔨
- 지난 주 작성하였던 아키텍처에서 퍼사드/서비스 클래스를 급하게 작성하느라, 내부의 책임이 너무 과중되어 객체지향적인 코드를 작성하지 못하였습니다.
- 관련하여, 테스트 코드를 다시 작성하고 도메인 정책과 각 클래스에 대한 책임을 최대한 나누며, 퍼사드 클래스를 다시 작성해보았습니다.

리뷰 포인트 🤔
1. 실무에서의 기능별 통합 테스트??
- InfraStructure에 대한 더미데이터를 기반으로 테스트 한다면,,, 보통 실무에서는 어떻게 하시나요?
- @Sql 어노테이션을 기반으로 더미데이터 쿼리를 넣어서 해당 데이터를 기반으로 jpa함수를 호출하였는데,, 굉장히 의미 없는 테스트를 작성한 기분입니다..
- 해당 데이터를 기반으로 각 계층에 대한 테스트를 모두 작성하거나,`필요한 예외`/`핵심 기능`을 기반으로 다시 작성해야 했을 것 같은데,, 개인적으로 다소 아쉬움이 남습니다.

2. JPA에서의 인덱스 선정방법?
- 결국에 쿼리를 만들고, 실행 계획을 보려면 해당 DB에 종속적으로 쿼리를 작성할 수 밖에 없다고 생각합니다..
- JPA를 기반으로 날아갈 쿼리를 미리 확인하고, MySQL에서 인덱스를 걸고 제외하면서, 실행계획을 확인하였는데
- Raw 하게 DB기반의 인덱스 적용 예상 결과를 확인한 후에 JPA 어노테이션을 통해 인덱스를 거는 이 흐름이 맞는지 궁금합니다.
- 코치님 께서는 기본 JPA 엔티티 설계 후, 이런 조회 성능 혹은 추가 쿼리 병목 지점을 어떻게 예상하시고, 인덱스를 적용할 계획을 수립하는지 노하우가 있으신가요??
- (또는 서비스 운영 후, 병목 지점을 후속적으로 조치하시나요??)

---
#### STEP07
- [O] 테이블 구조 개선안 (선택)
- [O] Infrastructure Layer 구성
- [△] 기능별 통합 테스트

#### STEP08
- [O] 조회 성능 저하 기능 식별
- [O] 쿼리 실행계획 기반 문제 분석
- [O] 인덱스/쿼리 재설계 및 개선안 도출

### **간단 회고** (3줄 이내)
- **잘한 점**: 퍼사드 클래스의 책임을 분산하고, 그에 대한 테스트 코드를 좀 더 자세히 작성해보려 했습니다. 테스트 코드를 먼저 작성하면서, 오류가나는 위치를 빠르게 접근하여 해결하는 방법이 매우 좋게 다가옵니다.
- **어려운 점**: E2E 테스트 코드 작성하기
- **다음 시도**: 아직 고도화되거나 미완성된 기능을 추가로 더 예쁘게 작성해보기..